block/device.rs 提供函数直接调用device接口对设备进行读写

block/io.rs 提供函数交互cache的读写，读写都先操作cache,若对应块的cache则调用设备接口载入cache， 
但io.rs所有函数都需要提供vec buffer, 从vec buffer读入数据写到cache 或者从cache读出数据写到vec buffer

block/handle 可以提供对某块cache的引用， 保证一致性 

1. 调整上层从device读写块的方式， 应当以使用block handle 为主， 直接持有并操作cache， 
而不是声明一个vec数组然后调用read_bytes write_bytes, 这样仅仅是将cache数据拷贝到vec中， 可能存在不一致问题

2. block handle中会确认是否启用cache， 而在其调用的read_block中会再次进行确认， 是否可以简化

3. 提供了不使用块cache的模式， 但是感觉很不靠谱， 从数据一致性各个方面来讲都不靠谱， 需要进一步测试或者直接不再提供这个模式 

4. 考虑inode block_group 这两个包装还是否有用，似乎已经被inoderef gpref给替代了

5. 考虑是否将crc提取出来作为单独模块

6. 将不必要的目录的跟踪从git中移除 如lwext4_core/target

7. 挖掘lwext4的目录索引逻辑，ext4的目录索引最初都是线性索引，文件数增多到一定程度才会转为htree,这在lwext4中如何体现，如何从线性变换到htree 以及htree的level如何增长 ，以及每一级映射中存储的hash-block如何分配， 尤其是hash如何分配

8. 判断是否有必要实现索引递归分裂， 按理说lwext4最多也只到level1， 只存在两种分裂情况， 只需要一个分裂函数就能解决， 没有需要处理递归分裂的场景

9. 考虑为extent 的ext4_new_meta_blocks、 ext4_ext_free_blocks 提供比lwext4更多的策略（比如提供更接近linux的实现）， 目前lwext4中这两个函数的count、flags参数都为扩展功能预留

10. 实现transaction的getblock与setblock,比如ext4_trans_block_get函数，与lwext4类似，将获取与释放block与日志结合， 或者可以用rust特性更优雅地实现释放

11. extent存放的问题 ext4_ext_can_prepend中为什么只判断x1是否已写入,而不继续判断x2， ext4假定x1 x2写入属性相同，为什么, 不同写入属性的extent能否共存在一个索引节点中？能否合并？

12. 检查lwext4中ext4_ext_insert_leaf前置合并的ext4_ext_correct_indexes问题，看看是否存在bug

13. 检查是否为cache buf提供了flag的实现，如ext4_bcache_set_flag(block->buf, BC_TMP);

14. 为arceos应用上lwext4可以提供的功能，而不是单纯的读写， 比如应用上xattr 实现文件保护、访问权限控制; 提供日志相关接口 ; mkfs ; mbr  

15. 解决可能存在的架构问题，使其可以应用在多种架构上

16. clippy代码检查

17. 接下来应该参照lwext4的ext4.c再实现一层封装，还是考虑直接尝试将当前项目接入arceos

18. 目前存在的问题：
- inode inoderef 职责边界不明确， inode偏向拷贝块缓存到vec buffer以及从vec buffer拷贝到块缓存， 而inoderef偏向持有块缓存引用， 一致性更好, inode作为拷贝应该专注于作为视图， 而inoderef则用于需要修改的地方， 保证一致性 
- block_group 与 block_group_ref 职责边界不明确， 
- journal模块已经大致实现，但没有很好地整合, 后续需要全面提供日志模块整合， 比如extent中的ExtentWriter
- bitmap虽然只提供一些简单的函数， 但是有一些还没有被使用到 ，可以尝试进行优化
- extent 继续完整实现extent的合并功能, 完善extent的节点分裂\生长， find_next_allocated_block只支持深度为0时查找, get_blocks,insert_extent_simpl函数支持连续数据块分配， 更好地发挥extent的优势， 修改extent全面支持大于0的深度， 考虑优化extent性能，如配合unwritten实现延迟分配
- dir htree实现还不够完整 dir中有需要联动extent的地方， 比如为htree分配新块时需要修改extent, 目前这部分内容不够完整, 另外还有write.rs中的迷之TODO,  dir中实现的功能与vfs层有一些重叠， 需要优化, 比如path_lookup.rs
- ✅ xattr的实现已重构完成，使用inoderef、block handle等保障一致性的工具。inode内部xattr完全实现（读写删除），xattr block的读取完全实现。xattr block的写入部分实现（COW逻辑、块分配逻辑还需在更高层实现）。所有xattr测试通过（24个测试） TODO：完善并整合校验， 尝试利用上hash模块提供xattr块共享
- ❌ META_BG 磁盘布局问题（已完成分析，见 META_BG_LAYOUT_ISSUES_REPORT.md）
  发现的严重问题：
  1. ❌ inode/write.rs:39 - write_inode 硬编码 GDT 位置（P0 严重）
  2. ❌ fs/block_group_ref.rs:80 - BlockGroupRef::get 硬编码 GDT 位置（P0 极严重，影响所有块分配）
  3. ❌ block_group/write.rs:37 - write_block_group_desc 硬编码 GDT 位置（P1 严重）
  4. ⚠️ superblock/write.rs:38 - 只写主 superblock，缺少备份写入（P2 中等）
  正确实现的代码：
  ✅ block_group/read.rs::read_block_group_desc - 完整 META_BG 支持
  ✅ superblock/read.rs - 完整 SPARSE_SUPER 支持
  修复建议：见报告第 4 节详细方案 
- c_api模块在初始时考虑作为兼容性的一环， 开发过程中没有完善， 现在考虑直接删除
- utils模块也没有发挥作用， 考虑删除
- 不少模块的crc32还没有真正整合, 比如extent模块, 有些地方则在验证校验和后直接忽略报错， 继续程序, 如try_alloc_in_group函数 
- 并不是所有cache ref相关的操作都完全放在闭包中， 交由对应的接口处理， 有些地方使用闭包实现拷贝的效果， 然后处理拷贝的数据， 这样可能会带来不一致的问题, 比如try_alloc_in_group中使用闭包获取了块组描述符的拷贝, 但blockdev只能同时存在一份mut引用， 或许是多虑
- 项目整体接口排查， 有一些内部函数没有被使用过， 有一些内部函数过时了， 有一些外部函数还没有被提供， 有一些外部函数