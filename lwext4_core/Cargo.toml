[package]
name = "lwext4_core"
version = "0.1.0"
edition = "2021"

[dependencies]
# 基础依赖
log = { version = "0.4", default-features = false }

# 字节序处理
byteorder = { version = "1.5", default-features = false }

# 位操作
bitflags = "2.4"

[features]
default = []
std = []
